[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "yt_comment_analyzer"
version = "0.0.1"
description = "A short description of the project."
authors = [
  { name = "Sovith" },
]

readme = "README.md"
classifiers = [
    "Programming Language :: Python :: 3",
    
]
requires-python = "~=3.13.0"


[tool.ruff]
line-length = 99
src = ["yt_comment_analyzer"]
include = ["pyproject.toml", "yt_comment_analyzer/**/*.py"]

[tool.ruff.lint]
extend-select = ["I"]  # Add import sorting

[tool.ruff.lint.isort]
known-first-party = ["yt_comment_analyzer"]
force-sort-within-sections = true

